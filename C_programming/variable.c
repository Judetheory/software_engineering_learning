#include <stdio.h>
/**
 * create variable
 * print the value in the expression
 * return 0 when successful
 */

int main ()

{
	int x = 10;
	int y = x / 2;
	printf("The magic number is: %i\nWhile the manipulative variable is %i\n", y, x);
	return (0);
}	
