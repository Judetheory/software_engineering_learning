#include <stdio.h>

int factorial(int number)
{
	int factorial = 1;
	
	for (int i = number; i > 1; i--)
	{
		factorial *= 1;
	return factorial;
	}
	printf("The factorial of %d is %d \n", number, factorial);		
}
